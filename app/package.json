{
  "name": "mobile-app-take-photo",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "yarn parcel index.html",
    "build": "yarn parcel build --dist-dir ../ index.html"
  },
  "devDependencies": {
    "parcel": "^2.8.2"
  },
  "dependencies": {
    "image-capture": "^0.4.0"
  }
}
